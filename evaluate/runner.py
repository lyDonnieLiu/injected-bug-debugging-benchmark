"""Unified attribution runner (design doc §6.1–§6.2).

Placeholder module: run every baseline over the head + MLP component space
and emit a ranked component list with effect estimates; SAE feature-level
results are mapped into the main space via feature → head projection and
additionally reported in the constrained subspace.
"""